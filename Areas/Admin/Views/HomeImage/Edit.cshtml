@model HomeImageModel
@{
    ViewData["title"] = "Create Image";
}
<div class="container-fluid">
    <h3 class="page-title">Chỉnh Sửa Hình ảnh</h3>
    <div class="form-container col-md-9">
        <form asp-action="Edit" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label for="Name">Tên Hình ảnh</label>
                <input asp-for="Name" class="form-control" id="Name" />
                <span asp-validation-for="Name" class="text-danger" />
            </div>
            <div class="form-group">
                <label for="Description">Mô tả</label>
                <textarea asp-for="Description" class="form-control" id="Description"></textarea>
                <span asp-validation-for="Description" class="text-danger" />
            </div>
            <div class="form-group">
                <label for="Status">Trạng thái</label>
                <select asp-for="Status" class="form-control" id="Status">
                    <option value="1">Hiển thị</option>
                    <option value="0">Ẩn</option>
                </select>
                <span asp-validation-for="Status" class="text-danger" />
            </div>
            <div class="form-group">
                <label>Hình ảnh</label>
                <input asp-for="ImageUpLoad" type="file" class="form-control-file" id="ImageUpLoad" />
                <span asp-validation-for="ImageUpLoad" class="text-danger" />
                <img id="imgpreview" class="pt-2" />
                <img src="~/assets/images/banner/@Model.Image" class="img-thumbnail" width="100" />
            </div>
            <button type="submit" class="btn btn-primary">Cập Nhật</button>
        </form>
        <a asp-action="Index" class="btn btn-secondary mt-3">Trở về Danh sách thương hiệu</a>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $("#ImageUpLoad").change(function () {

            readURL(this);
        })
    </script>
    <script src="https://cdn.ckeditor.com/4.22.1/standard/ckeditor.js"></script>
    <script>
        CKEDITOR.replace('Description');
    </script>
}
